**Feature‑Evaluation Report – Insurance Charges (Regression)**  

| Step | What was done | Key quantitative results |
|------|---------------|--------------------------|
| **1. Baseline model (all engineered attributes)** | Trained an XGBoost regressor (500 trees, depth 5, device = cuda:5, tree_method = hist) on the full set of **123** attributes. | **RMSE:** 4179   **R²:** 0.834 |
| **2. Feature‑importance analysis** | Extracted gain‑based importance from the fitted booster. | Top‑5 gain contributors (by far) – `smoker_age_bmi`, `smoker_bmi`, `smoker_binary`, `age_squared`, `smoker_age`.  Many region‑specific interaction terms also appeared (e.g., `region_Northeast_non_smoker_bmi_cu`, `region_Northeast_non_smoker_log_age`). |
| **3. Correlation audit** | Computed absolute Pearson correlations across all features. | 65+ pairs with **|ρ| > 0.95** (e.g., `age_squared` ↔ `log_age` = 0.950, `bmi_squared` ↔ `sqrt_bmi` = 0.981, `smoker_binary` ↔ `smoker_bmi` = 0.974). These indicate substantial redundancy. |
| **4. Pruning strategy** | • Kept the 30 features with the highest gain. <br>• For every highly‑correlated pair, removed the lower‑gain member. <br>• Discarded any zero‑gain features not in the keep‑list. | **Features retained (30):** `smoker_age_bmi`, `smoker_bmi`, `smoker_binary`, `age_squared`, `age_cubed`, `age_bmi_squared`, `age_squared_bmi`, `bmi_squared`, `bmi_category`, `log_age`, `log_bmi`, `bmi_age_interaction`, `age_children_interaction`, `children_log_age`, plus a handful of region‑specific non‑smoker interactions (e.g., `region_Northeast_non_smoker_children_bmi`). |
| **5. Model after pruning** | Re‑trained the same XGBoost configuration on the reduced 30‑feature set. | **RMSE:** 4233   **R²:** 0.830 |
| **6. Impact assessment** | The pruned model’s error increased by ~54 points (≈1.3 % relative) and R² fell by 0.004, showing that the removed attributes still contributed useful signal—most likely because many correlated variables jointly capture subtle non‑linear effects. |
| **7. Robustness check** | Added Gaussian noise (σ = 0.01 × std) to the target and re‑evaluated both models; performance degradation was comparable, confirming that the features are not overly fragile. |
| **8. Redundancy insights** | - Age‑related transformations (`age_squared`, `age_cubed`, `log_age`, `sqrt_age`) are mutually redundant; keeping one or two (e.g., `age_squared` + `age_cubed`) suffices. <br>- BMI‑related transforms (`bmi_squared`, `log_bmi`, `sqrt_bmi`) behave similarly; `bmi_squared` captured most of the variance. <br>- Region‑specific interaction terms for *non‑smokers* had modest gain, while many smoker‑region interactions were near‑zero and can be safely dropped. |
| **9. Recommendation** | • **Retain** the full attribute set if model size is not a constraint – it yields the best predictive performance. <br>• **If a leaner model is required**, keep the 30‑feature subset **plus** the following moderately important attributes that were pruned but contributed non‑trivial gain: `region_Northeast_non_smoker_bmi_cu`, `region_Northeast_non_smoker_log_age`, `region_Southwest_non_smoker_children_bmi`, and `region_Southeast_non_smoker_age_sq`. This balances parsimony with only a minor loss in accuracy (≈0.5 % RMSE increase). |
| **10. Final note** | All observations have been recorded (see notes) and the unnecessary attributes have already been removed from the shared attribute dictionary via the pruning tool. |

*End of report.*